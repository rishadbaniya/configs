alias ls="exa"
alias cat="bat"
alias c="clear"
alias tree="et"

set -x FNM_DIR $HOME/.local/share/fnm
set -x LOGSEQ_DIR $HOME/.local/logseq
set -x PATH $PATH $FNM $LOGSEQ_DIR

#if status is-interactive
#    # Commands to run in interactive sessions can go here
#end


